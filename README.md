# New App

Minimal Vite + React + TypeScript scaffold. Renders a single header with text "New". No CSS included.

Run locally:

```bash
cd new-app
npm install
npm run dev
```

Open <http://localhost:5173>
