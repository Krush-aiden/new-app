Place static images, icons, and other module-importable assets here.

Import like:

```ts
import logo from './assets/logo.png'
```

Or place fully static files in `public/` and reference them as `/filename.png` in HTML or CSS.
